---
- hosts: node
  become: true
  # vars: 
  #   master_ad: "{{groups['master'] | join(',')}}" 
  tasks:
    - ansible.builtin.debug:
        # var: master_ad
        msg: "{{hostvars[groups['master'] | join(',')].ansible_hostname}}"
      #loop: "{{groups['master']}}"
     